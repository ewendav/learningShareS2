<?xml version="1.0"?>
<ruleset name="LearningShareS2">
    <description>Custom PHPCS Rules</description>
    <rule ref="PSR12"/>
    <rule ref="PSR1"/>

    <!-- Exclure des dossiers -->
    <exclude-pattern>vendor/*</exclude-pattern>
    <exclude-pattern>node_modules/*</exclude-pattern>

    <!-- Autoriser des extensions spécifiques -->
    <fileExtensions>
        <ext>php</ext>
    </fileExtensions>
</ruleset>